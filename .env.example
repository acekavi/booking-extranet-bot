# Only needed if you use the "postgres" price source.
# The tool never asks for Booking.com credentials: you log in yourself via
# `npm run login`, and only the resulting browser session is stored.
DATABASE_URL=postgres://user:password@localhost:5432/your_database
